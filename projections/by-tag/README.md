# by-tag/ — Tag-scoped projection slices

Tag-scoped projection files go here. Each file is a filtered subset of the master projection, containing only concepts tagged with a specific tag (plus the ontology §0).

Use tag slices when you want a focused cloud LLM Project on one topic area, rather than loading the full bundle.
